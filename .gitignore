# === Node / npm ===
node_modules/
npm-debug.log*
yarn-error.log*
package-lock.json      # keep root lockfile, see below
!package-lock.json     # negate the ignore for the root file
**/package-lock.json   # ignore any nested lockfiles (e.g. csv-parse/package-lock.json)

# === dotenv / secrets ===
.env
.env.*
!.env.example

# === OS & editor junk ===
.DS_Store
Thumbs.db
.idea/
.vscode/

# === test & coverage artifacts ===
coverage/
*.lcov

# === local CSV dumps, temp ===
*.csv
*.tsv
*.tmp